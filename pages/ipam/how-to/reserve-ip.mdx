---
title: How to reserve an IP address
description: Learn how to reserve public and private IP addresses with Scaleway's IPAM. This step-by-step guide helps you allocate IPs in advance, for later use with your Scaleway resources.
tags: ipam ip-address-manager private-ip-address public-ip-address ip-address reserve static
dates:
  validation: 2026-08-27
  posted: 2024-09-05
---
import Requirements from '@macros/iam/requirements.mdx'


**IP** **A**ddress **M**anager (IPAM) is Scaleway's tool for planning, tracking and managing the IP address space of Scaleway products. It acts as a single source of truth for the IP addresses of Scaleway resources.

You can use IPAM to reserve an IP address from any of the following sources:

- **Private IPs**: addresses from the [CIDR block](/vpc/concepts/#cidr-block) of one of your Private Networks. Reserving such an address prevents it from being auto-allocated to new resources attached to the Private Network, and lets you use it to attach the specific resource of your choice.
- **Public regional IPs**: public addresses scoped to a whole region, such as `fr-par`.
- **Public zonal IPs**: public addresses scoped to a single Availability Zone, such as `fr-par-1`.

A reserved IP address has a lifecycle of its own, separate from the resource it is attached to. If you detach or delete the resource, the address stays reserved, until you either release it or use it to attach a different resource.

<Message type="note">
Currently, only CPU & GPU Instances, Elastic Metal servers, Load Balancers, and Public Gateways are compatible with reserved private IPs. Support for Managed Databases is planned.
</Message>

<Requirements />

- A Scaleway account logged into the [console](https://console.scaleway.com)
- [Owner](/iam/concepts/#owner) status or [IAM permissions](/iam/concepts/#permission) allowing you to perform actions in the intended Organization
- At least one [Private Network](/vpc/how-to/create-private-network/), if you want to reserve a private IP address

## Reserve an IP address

1. Click **IPAM** in the **Network** section of the [Scaleway console](https://console.scaleway.com) side menu.

2. Select the region in which you want to reserve an IP address, using the drop-down menu at the top of the page.

3. Click **Reserve IP**.

4. Select the **source** of the IP address to reserve: **Private IPs**, **Public zonal IPs**, or **Public regional IPs**.

5. Define the remaining parameters, which depend on the source you selected:

    - For a private IP: the **VPC** and the **Private Network** to reserve the address from. The reserved IP comes from the [CIDR block](/vpc/concepts/#cidr-block) of this network, and is available to attach resources to it.
    - For a public zonal IP: the **Availability Zone** to reserve the address in.
    - For a public regional IP: no further scope is required, as the address is available across the whole selected region.

6. Select the **IP type**: **IPv4** or **IPv6**.

7. Optionally, add **tags** to help you filter and manage your reserved IPs later.

    <Message type="tip">
    Four IP addresses from each Private Network CIDR block are unavailable for reservation: the first two and last two in the block. For example, for the subnet `172.16.12.0/22` the following addresses cannot be reserved or assigned to any resource: `172.16.12.0`, `172.16.12.1`, `172.16.15.254` and `172.16.15.255`.
    </Message>

8. Check the **Summary**, then click **Reserve** to reserve the IP.

Your new IP address appears in the list of IP addresses, with the **Reserved** status.
