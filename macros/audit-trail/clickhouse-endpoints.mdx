---
macro: adt-clickhouse-endpoints
---

<Accordion>
  ## ClickHouse®

  ### ClickHouse® supported endpoints

    The following [ClickHouse®](/data-warehouse/) endpoints are supported and will be logged in Audit Trail:

    - CreateDeployment (`POST /datawarehouse/v1beta1/regions/{region}/deployments`)
    - UpdateDeployment (`PATCH /datawarehouse/v1beta1/regions/{region}/deployments/{deployment_id}`)
    - StartDeployment (`POST /datawarehouse/v1beta1/regions/{region}/deployments/{deployment_id}/start`)
    - StopDeployment (`POST /datawarehouse/v1beta1/regions/{region}/deployments/{deployment_id}/stop`)
    - DeleteDeployment (`DELETE /datawarehouse/v1beta1/regions/{region}/deployments/{deployment_id}`)
    - CreateEndpoint (`POST /datawarehouse/v1beta1/regions/{region}/endpoints`)
    - DeleteEndpoint (`DELETE /datawarehouse/v1beta1/regions/{region}/endpoints/{endpoint_id}`)
    - CreateUser (`POST /datawarehouse/v1beta1/regions/{region}/deployments/{deployment_id}/users`)
    - UpdateUser (`PATCH /datawarehouse/v1beta1/regions/{region}/deployments/{deployment_id}/users/{name}`)
    - DeleteUser (`DELETE /datawarehouse/v1beta1/regions/{region}/deployments/{deployment_id}/users/{name}`)
    - CreateDatabase (`POST /datawarehouse/v1beta1/regions/{region}/deployments/{deployment_id}/databases`)
    - DeleteDatabase (`DELETE /datawarehouse/v1beta1/regions/{region}/deployments/{deployment_id}/databases/{name}`)
</Accordion>