---
title: Scaleway Instances Lifecycle
description: Learn about the shared responsibility model for Scaleway Instances services, outlining the roles of Scaleway and users in managing Instances security, backups, and compliance.
tags: instances srm
dates:
  validation: 2026-01-12
  posted: 2026-01-12
---

The lifecycle of Scaleway Instances breaks down into two main aspects: the **operational lifecycle** of an individual Instance (its day-to-day states and transitions) and the **product-level lifecycle** of Instance *types/generations* (how Scaleway evolves and retires hardware offerings).

### 1. Operational Lifecycle of an Individual Instance
This is the most common meaning of “instance lifecycle” in cloud platforms (similar to AWS EC2 or GCP Compute Engine). It covers the full journey from creation to deletion, managed via the Scaleway console, API, CLI, or Terraform.

#### Key States / Power Modes
- **Running (Started)**: The Instance is fully active, consuming compute resources. This is the default after creation or power-on. Full billing applies (compute + storage + IPs).
- **Standby**: The Instance is halted (powered down) but remains allocated to your account. Data stays on local storage for fast restart. Quick to resume, but **billed exactly like a running Instance**. Use this for short-term pauses.
- **Powered Off (Stopped)**: All local-volume data is automatically archived to a volume store; the physical hypervisor node is released back into Scaleway’s pool. Compute billing stops (you still pay for any Block Storage volumes and Flexible IPs). Restarting restores the data automatically. This is the recommended state for long-term cost savings.
- **Error / Locked / Protected**: Temporary or administrative states (e.g., during resize, rescue mode, or when you enable protection to prevent accidental stop/delete).
- **Terminated (Deleted)**: The Instance and its local volumes are permanently removed. Flexible IPs attached to it are preserved unless you release them manually.


### 2. Product-Level Lifecycle of Instance Types & Generations
Scaleway continuously refreshes its hardware for performance, sustainability, and new features (e.g., better CPUs, live migration). Old generations follow a clear deprecation path:

**General Availability (GA)**: Fully launched and supported product. Ready for production use by all customers. Full Service Level Agreement (SLA) applies.
**End of Deployment**: Offer don't receive new hardware, offers still receive features, patches & bugfixes.
**End of New Features**: Offers don't receive new features except for security patches & bugfixes
