---
macro: adt-mongodb-endpoints
---

<Accordion>
  ## MongoDB®

  ### MongoDB® supported endpoints

    The following [MongoDB®](/managed-mongodb-databases/) endpoints are supported and will be logged in Audit Trail:

    - CreateInstance (`POST /mongodb/v1/regions/{region}/instances`)
    - UpdateInstance (`PATCH /mongodb/v1/regions/{region}/instances/{instance_id}`)
    - DeleteInstance (`DELETE /mongodb/v1/regions/{region}/instances/{instance_id}`)
    - UpgradeInstance (`POST /mongodb/v1/regions/{region}/instances/{instance_id}/upgrade`)
    - CreateSnapshot (`POST /mongodb/v1/regions/{region}/snapshots`)
    - UpdateSnapshot (`PATCH /mongodb/v1/regions/{region}/snapshots/{snapshot_id}`)
    - DeleteSnapshot (`DELETE /mongodb/v1/regions/{region}/snapshots/{snapshot_id}`)
    - RestoreSnapshot (`POST /mongodb/v1/regions/{region}/snapshots/{snapshot_id}/restore`)
    - ApplyMaintenance (`POST /mongodb/v1/regions/{region}/maintenances/{maintenance_id}/apply`)
    - CreateEndpoint (`POST /mongodb/v1/regions/{region}/endpoints`)
    - DeleteEndpoint (`DELETE /mongodb/v1/regions/{region}/endpoints/{endpoint_id}`)
    - CreateUser (`POST /mongodb/v1/regions/{region}/instances/{instance_id}/users`)
    - UpdateUser (`PATCH /mongodb/v1/regions/{region}/instances/{instance_id}/users/{name}`)
    - DeleteUser (`DELETE /mongodb/v1/regions/{region}/instances/{instance_id}/users/{name}`)
    - SetUserRole (`PUT /mongodb/v1/regions/{region}/instances/{instance_id}/set-user-roles`)
</Accordion>